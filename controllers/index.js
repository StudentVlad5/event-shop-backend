const auth = require('./auth');
const users = require('./users');
const events = require('./events');
const packages = require('./categories');

module.exports = {
  auth,
  users,
  events,
  packages,
};
